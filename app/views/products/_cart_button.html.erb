<%= form_tag order_products_path, method: 'post' do |f| %>
  <%= hidden_field_tag :id, product.id %>
  <%= hidden_field_tag :additional_product, product.id if defined? additional_product %>
  <%= submit_tag 'Add To Cart', class: 'btn btn-lg btn-cart' %>
<% end %>