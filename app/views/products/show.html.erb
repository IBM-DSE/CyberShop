<h1><%= @product.name %></h1>
<p>by <%= @product.brand.name %></p>

<%= content_tag :p, image_tag(@product.image.url) %>
<br>
<%= content_tag :h4, @product.deal.description if @product.deal %>
<%= render partial: 'button', locals: {product: @product} %>