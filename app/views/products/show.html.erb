<h1><%= @product.name %></h1>
<h4>by <%= @product.brand.name %></h4>
<%= content_tag :p, image_tag(@product.image.url) %>

<%= @product.deal if @product.deal %>

<%= render @deals %>

<p>
<%= render partial: 'cart_button', locals: { product: @product} %>
</p>