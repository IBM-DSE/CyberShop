<%= form_for current_customer.messages.build, remote: true do |f| %>
    <%= f.text_field :content, id: 'chat-input', placeholder: 'Send a message...' %>
    <%= f.hidden_field :context, value: {name: current_customer.name, interest: 'smartphones'}.to_json %>
<% end %>